import controller from './Test/test.controller';
import template from './Test/test.template.html';

const bindings = {
    sampleData: '<'
};

export default {
    controller,
    template,
    bindings
};