import controller from './ControlLimits/controllimits.controller';
import template from './ControlLimits/controllimits.template.html';

const bindings = {
    limits: '<'
}

export default {
    template,
    controller,
    bindings
}

